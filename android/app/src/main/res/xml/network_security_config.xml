<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow cleartext (HTTP) traffic only to your backend servers for development/testing -->
    <!-- ⚠️  WARNING: This is ONLY for development. Remove before production! -->

    <!-- Allow HTTP to DigitalOcean backend (development only) -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">142.93.94.236</domain>
    </domain-config>

    <!-- Allow HTTP to localhost for local testing -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>

    <!-- Allow HTTP to local network IPs for testing -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">192.168.0.0/16</domain>
        <domain includeSubdomains="true">10.0.0.0/8</domain>
    </domain-config>

    <!-- All other traffic must use HTTPS (secure default) -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">*</domain>
    </domain-config>
</network-security-config>
