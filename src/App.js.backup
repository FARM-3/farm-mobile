import React from 'react';
import { StatusBar } from 'expo-status-bar';
import { StyleSheet, View } from 'react-native';
import { StyleSheet, View, SafeAreaView } from 'react-native'; // <--- Import SafeAreaView

// 1. Import your main screen component
import DashboardScreen from './src/features/dashboard/screens/DashboardScreen';

// 2. Import your defined colors for the status bar style
import CoffeeColors from './src/theme/colors';

export default function App() {
  return (
    <View style={styles.container}>
      {/* 3. Render your DashboardScreen */}
      <DashboardScreen />
      
      {/* Set the status bar style based on your theme */}
      <StatusBar style="light" backgroundColor={CoffeeColors.DARK_BROWN} />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    // The main container should just take up all available space
    flex: 1, 
    // Remove background color, alignment, and justification from here 
    // as they are handled by the screens and navigators
  },
});
